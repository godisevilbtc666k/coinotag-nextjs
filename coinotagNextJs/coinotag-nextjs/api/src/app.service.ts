import { Injectable } from '@nestjs/common';

@Injectable()
export class AppService {
  // İleride sağlık kontrolü (health check) fonksiyonu eklenebilir
  // getHealth(): string {
  //   return 'API Gateway is running!';
  // }
} 